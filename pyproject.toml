[build-system]
build-backend = "hatchling.build"
requires = [ "hatchling" ]

[project]
name = "django-admin-radio-select"
version = "0.1.0"
description = "Synchronize BooleanFields across Django Admin inline rows as mutually exclusive radio button groups."
readme = "README.md"
license = "MIT"
license-files = [ "LICENSE" ]
authors = [ { name = "Rodolfo Becerra", email = "rodolvbg@gmail.com" } ]
requires-python = ">=3.10"
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Web Environment",
  "Framework :: Django",
  "Framework :: Django :: 4.2",
  "Framework :: Django :: 5.0",
  "Framework :: Django :: 5.1",
  "Framework :: Django :: 5.2",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3.15",
  "Typing :: Typed",
]
dependencies = [ "django>=4.2" ]
optional-dependencies.dev = [
  "coverage[toml]>=7.4",
  "django-stubs[compatible-mypy]>=5",
  "mypy>=1.10",
  "pre-commit>=4",
  "pytest>=8",
  "pytest-cov>=5",
  "pytest-django>=4.8",
  "pytest-playwright>=0.5",
  "ruff>=0.6",
  "tox>=4",
  "tox-uv>=1",
]
urls.Homepage = "https://github.com/rodolvbg/django-admin-radio-select"
urls.Repository = "https://github.com/rodolvbg/django-admin-radio-select"

[dependency-groups]
dev = [
  "coverage[toml]>=7.4",
  "django-stubs[compatible-mypy]>=5",
  "mypy>=1.10",
  "pre-commit>=4",
  "pytest>=8",
  "pytest-cov>=5",
  "pytest-django>=4.8",
  "pytest-playwright>=0.5",
  "ruff>=0.6",
  "tox>=4",
  "tox-uv>=1",
]

[tool.hatch]
build.targets.wheel.packages = [ "src/django_admin_radio_select" ]

[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [ "**/migrations/*.py" ]
lint.select = [ "B", "DJ", "E", "F", "I", "UP" ]

[tool.mypy]
mypy_path = "src:example"
python_version = "3.10"
warn_redundant_casts = true
warn_unused_ignores = true
plugins = [ "mypy_django_plugin.main" ]

[tool.django-stubs]
django_settings_module = "example.settings"

[tool.pytest]
ini_options.testpaths = [ "tests" ]
ini_options.pythonpath = [ "src", "example" ]
ini_options.addopts = "--no-migrations --cov=django_admin_radio_select --cov-report=term-missing"
ini_options.DJANGO_SETTINGS_MODULE = "example.settings"

[tool.coverage]
run.source = [ "django_admin_radio_select" ]
run.branch = true
report.skip_covered = true
report.show_missing = true

[tool.tox]
env_list = [
  "py314-dj52",
  "py313-dj51",
  "py312-dj42",
  "py312-dj50",
  "py310-dj42",
  "py310-dj50",
  "py310-dj51",
  "py310-dj52",
]
env_run_base.package = "wheel"
env_run_base.wheel_build_env = ".pkg"
env_run_base.deps = [
  "pytest",
  "pytest-django",
  "pytest-cov",
  { replace = "if", condition = "factor.dj42", then = [ "django>=4.2,<4.3" ], extend = true },
  { replace = "if", condition = "factor.dj50", then = [ "django>=5.0,<5.1" ], extend = true },
  { replace = "if", condition = "factor.dj51", then = [ "django>=5.1,<5.2" ], extend = true },
  { replace = "if", condition = "factor.dj52", then = [ "django>=5.2,<5.3" ], extend = true },
]
env_run_base.commands = [ [ "pytest", "tests", "--ignore=tests/e2e", "{posargs}" ] ]
