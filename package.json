{
    "name": "django-admin-radio-select",
    "private": true,
    "version": "0.1.0",
    "description": "JS test tooling for django-admin-radio-select",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^2.1.0",
        "jsdom": "^25.0.0",
        "vitest": "^2.1.0"
    }
}
