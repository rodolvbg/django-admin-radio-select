# django-admin-radio-select
A Django Admin package that turns BooleanFields across ModelAdmin or Inline rows into synchronized radio button groups, allowing mutually exclusive selection while preserving Django’s native forms and formsets.
